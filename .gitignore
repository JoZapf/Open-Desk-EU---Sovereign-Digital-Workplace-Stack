# Secrets — never commit
secrets/

# Environment files with credentials
*.env
.env.*

# Data directory (lives on /mnt/docker-data/opendesk/)
# Not in this repo, but just in case
data/

# Runbook contains secret values from terminal sessions
RUNBOOK*

# Archive - needs review before publishing
archive/

# Product Backlog - needs review before publishing
OPEN-DESK_Product_Backlog*
