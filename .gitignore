# Secrets — never commit
secrets/

# Environment files with deployment-specific values
.env
*.env
.env.*
!.env.example

# Data directory (lives on /srv/opendesk-data/)
# Not in this repo, but just in case
data/

# Runbook contains secret values from terminal sessions
RUNBOOK*

# Session handoffs (deployment-specific)
SESSION_HANDOFF_*
docs/SESSION_HANDOFF_*

# Verification reports (generated, deployment-specific)
verify-network-routing-*
docs/verify-network-routing-*

# Project instructions (deployment-specific conventions, usernames)
CLAUDE.md

# Archive/Scripts - contain deployment-specific values
archive/
scripts/ddns/
scripts/keycloak/
scripts/gather-network-info*

# Documentation - needs review before publishing
OPEN-DESK_Product_Backlog*
docs/ADR-*
docs/DNS*
docs/NO_NEW_PRIVILEGES*
